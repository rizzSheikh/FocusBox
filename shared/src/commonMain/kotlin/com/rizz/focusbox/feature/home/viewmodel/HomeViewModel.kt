package com.rizz.focusbox.feature.home.viewmodel

import androidx.lifecycle.ViewModel
import com.rizz.focusbox.feature.home.view.event.HomeScreenEvents

class HomeViewModel : ViewModel() {
    fun onEvent(event: HomeScreenEvents) {
        @Suppress("UNUSED_EXPRESSION")
        when (event) {
            else -> Unit
        }
    }
}
